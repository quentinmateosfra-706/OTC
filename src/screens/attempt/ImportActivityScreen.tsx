import React from 'react';
import { PlaceholderScreen } from '@/components/PlaceholderScreen';

/** Import d'une activité Strava + lancement de la validation — Phase 7. */
export function ImportActivityScreen() {
  return (
    <PlaceholderScreen
      title="Importer une activité"
      subtitle="Sélection Strava et validation (Phase 7)"
      icon="cloud-download-outline"
    />
  );
}
