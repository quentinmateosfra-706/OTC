import React from 'react';
import { PlaceholderScreen } from '@/components/PlaceholderScreen';

/** Connexion (email/Apple/Google) — implémentée en Phase 3. */
export function LoginScreen() {
  return (
    <PlaceholderScreen
      title="Connexion"
      subtitle="Email, Apple, Google (Phase 3)"
      icon="log-in-outline"
    />
  );
}
