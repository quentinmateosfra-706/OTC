import React from 'react';
import { PlaceholderScreen } from '@/components/PlaceholderScreen';

/** Connexion du compte Strava (OAuth) — implémentée en Phase 4. */
export function StravaConnectScreen() {
  return (
    <PlaceholderScreen
      title="Connexion Strava"
      subtitle="Autorisation OAuth obligatoire (Phase 4)"
      icon="link-outline"
    />
  );
}
