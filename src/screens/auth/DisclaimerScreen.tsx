import React from 'react';
import { PlaceholderScreen } from '@/components/PlaceholderScreen';

/**
 * Décharge de responsabilité (case non pré-cochée + horodatage) —
 * implémentée en Phase 3. Étape obligatoire à l'inscription.
 */
export function DisclaimerScreen() {
  return (
    <PlaceholderScreen
      title="Décharge de responsabilité"
      subtitle="Consentement obligatoire et horodaté (Phase 3)"
      icon="shield-checkmark-outline"
    />
  );
}
