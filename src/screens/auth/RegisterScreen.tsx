import React from 'react';
import { PlaceholderScreen } from '@/components/PlaceholderScreen';

/** Inscription + profil coureur — implémentée en Phase 3. */
export function RegisterScreen() {
  return (
    <PlaceholderScreen
      title="Inscription"
      subtitle="Création de compte et profil coureur (Phase 3)"
      icon="person-add-outline"
    />
  );
}
