import React from 'react';
import { PlaceholderScreen } from '@/components/PlaceholderScreen';

/** Catalogue des courses — implémenté en Phase 5. */
export function CatalogScreen() {
  return (
    <PlaceholderScreen
      title="Catalogue"
      subtitle="Liste des courses (Phase 5)"
      icon="map-outline"
    />
  );
}
