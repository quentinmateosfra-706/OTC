import React from 'react';
import { PlaceholderScreen } from '@/components/PlaceholderScreen';

/** Classement d'une course donnée (filtres catégorie) — Phase 8. */
export function RaceLeaderboardScreen() {
  return (
    <PlaceholderScreen
      title="Classement de la course"
      subtitle="Filtres H/F, âge, région (Phase 8)"
      icon="trophy-outline"
    />
  );
}
